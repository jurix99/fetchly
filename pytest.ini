[pytest]
# Make the repo root importable so tests can `from app import ...` without an
# install step. Tests live in tests/ and touch only stdlib + app (no network).
pythonpath = .
testpaths = tests
addopts = -q
